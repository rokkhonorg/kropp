name: Release

# When a version tag (v*) is pushed, build release binaries for each platform
# and attach them to a GitHub Release.
on:
  push:
    tags: ["v*"]

permissions:
  contents: write # needed to create the release and upload assets

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    name: Release (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            bin: kropp.exe
            suffix: windows-x86_64
          - os: ubuntu-latest
            bin: kropp
            suffix: linux-x86_64
          - os: macos-latest
            bin: kropp
            suffix: macos-arm64
    steps:
      - uses: actions/checkout@v7

      # SAM3 pulls C++ deps (esaxx-rs / onig) that link against clang's
      # compiler-rt; pin a stable Xcode so its clang resource path resolves on
      # the macOS runner.
      - name: Select Xcode (macOS)
        if: runner.os == 'macOS'
        uses: maxim-lobanov/setup-xcode@v1.7.0
        with:
          xcode-version: latest-stable

      # The cc crate emits `-lclang_rt.osx` with a guessed clang resource path
      # that doesn't exist on the runner, and the link uses `-nodefaultlibs` so
      # there's no fallback search. Find the real libclang_rt.osx.a and add its
      # directory to the link search path.
      - name: Provide clang runtime path (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          libdir=""
          for base in \
            "$(xcode-select -p)/Toolchains/XcodeDefault.xctoolchain/usr/lib/clang" \
            "/Library/Developer/CommandLineTools/usr/lib/clang"; do
            [ -d "$base" ] || continue
            hit="$(find "$base" -name libclang_rt.osx.a 2>/dev/null | head -1 || true)"
            if [ -n "$hit" ]; then libdir="$(dirname "$hit")"; break; fi
          done
          if [ -z "$libdir" ]; then
            echo "could not find libclang_rt.osx.a" >&2
            exit 1
          fi
          echo "clang runtime dir: $libdir"
          echo "RUSTFLAGS=${RUSTFLAGS:-} -L $libdir" >> "$GITHUB_ENV"

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release --locked

      # Package the binary with the bundled orientation model (so drag-and-drop
      # finds onnx/bkori.onnx next to the exe) plus the licence and readme.
      - name: Package (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          ASSET="kropp-${GITHUB_REF_NAME}-${{ matrix.suffix }}.tar.gz"
          mkdir dist
          cp "target/release/${{ matrix.bin }}" LICENSE README.md dist/
          cp -r onnx dist/
          tar -czf "$ASSET" -C dist .
          echo "ASSET=$ASSET" >> "$GITHUB_ENV"

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $asset = "kropp-$env:GITHUB_REF_NAME-${{ matrix.suffix }}.zip"
          New-Item -ItemType Directory dist | Out-Null
          Copy-Item "target/release/${{ matrix.bin }}", LICENSE, README.md dist/
          Copy-Item -Recurse onnx dist/
          Compress-Archive -Path dist/* -DestinationPath $asset
          "ASSET=$asset" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Attach to release
        uses: softprops/action-gh-release@v3
        with:
          files: ${{ env.ASSET }}
          generate_release_notes: true
          fail_on_unmatched_files: true
